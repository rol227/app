<h1>Home</h1>

<p>
	<% if user_signed_in? %>
		<p>Logged in as: </p>
		<%= current_user.email %>
		<%= link_to 'Sign Out', destroy_user_session_path, method: :delete %>
	<% else %>
		<%= link_to 'Sign In', new_user_session_path %>
	<% end %>
</p>

<% @organizations.each do |org| %>
	<p>
		Name: 
		<%= org.name %> 
		<%= link_to 'Show', organization_path(org) %>
	</p>
<% end %>

<%= link_to 'New', new_organization_path %>
<%= link_to 'Sign Up', new_user_registration_path %>


$('#org-form').html("<%= j (render 'form') %>");
$('#org-form').slideDown(350);

<div id="next_group">
    <%= button_to 'Upload', new_organization_path, remote: true, method: :get %>
</div>
<div id="org-form"></div>

<a type="button" class="btn btn-social-icon btn-facebook" onclick="location.href='<%= organizations_path %>'">
    			<i class="fa fa-facebook-official fa-3x"></i>
  			</a>

<%= button_to new_organization_path, remote: true, method: :get do %>
        	<i class="fa fa-facebook-official fa-3x"></i>
        <% end %>