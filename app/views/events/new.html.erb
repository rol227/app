<h1>New Event</h1>

<%= form_for [@organization, @event] do |f| %>
	<p>
		<%= f.label :when %>
		<%= f.date_select :when %>
	</p>
		<%= f.label :vip_id %>
		<%= f.select :vip_id %>
	</p>
	<p>
		<%= f.submit %>
	</p>
<% end %>